<UserControl
    x:Class="CTime2.Controls.UserImage"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CTime2.Controls"
    xmlns:converter="using:CTime2.Converter"
    
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <UserControl.Resources>
        <converter:ByteArrayToImageSourceConverter x:Key="ByteArrayToImageSourceConverter" />
    </UserControl.Resources>
    <Grid>
        <Ellipse Stroke="LightGray"
                 StrokeThickness="2">
            <Ellipse.Fill>
                <ImageBrush ImageSource="{x:Bind Image, Mode=OneWay, Converter={StaticResource ByteArrayToImageSourceConverter}}" 
                            AlignmentX="Center" 
                            AlignmentY="Center" />
            </Ellipse.Fill>
        </Ellipse>
    </Grid>
</UserControl>
