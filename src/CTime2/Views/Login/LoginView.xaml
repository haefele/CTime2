<Page
    x:Class="CTime2.Views.Login.LoginView"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CTime2.Views.Login"
    
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:micro="using:Caliburn.Micro"
    mc:Ignorable="d"
    >

    <StackPanel>
        <TextBox Header="Company Id" Text="{Binding CompanyId, Mode=TwoWay}" />
        <TextBox Header="Email address" Text="{Binding EmailAddress, Mode=TwoWay}" />
        <PasswordBox Header="Password" Password="{Binding Password, Mode=TwoWay}" />
        
        <Button Content="Login" micro:Message.Attach="Login" />
    </StackPanel>
</Page>
