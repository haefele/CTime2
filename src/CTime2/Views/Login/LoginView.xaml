<Page
    x:Class="CTime2.Views.Login.LoginView"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CTime2.Views.Login"
    xmlns:micro="using:Caliburn.Micro"
    
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >
    <Grid Margin="5"
          Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox Header="Firmen ID" 
                 Grid.Row="0"
                 Text="{Binding CompanyId, Mode=TwoWay}" />
        <TextBox Header="E-Mail-Adresse" 
                 Grid.Row="2"
                 Text="{Binding EmailAddress, Mode=TwoWay}" 
                 InputScope="EmailNameOrAddress" />
        <PasswordBox Header="Passwort" 
                     Grid.Row="4"
                     Password="{Binding Password, Mode=TwoWay}" />
        
        <Button Content="Anmelden" 
                Grid.Row="6"
                micro:Message.Attach="Login"
                HorizontalAlignment="Stretch"/>
    </Grid>
</Page>
