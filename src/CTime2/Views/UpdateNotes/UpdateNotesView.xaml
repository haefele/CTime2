<Page
    x:Class="CTime2.Views.UpdateNotes.UpdateNotesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:uwp="using:FontAwesome.UWP"
    
    MaxWidth="320"
    MaxHeight="400">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Light">
                    <SolidColorBrush x:Key="BorderBrush" Color="#7A7A7A" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <SolidColorBrush x:Key="BorderBrush" Color="#797979" />
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" 
          Padding="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <uwp:FontAwesome Grid.Row="0"
                         Icon="InfoCircle" 
                         Opacity="0.2"
                         FontSize="60" />

        <Viewbox Grid.Row="1" StretchDirection="DownOnly" HorizontalAlignment="Left">
            <TextBlock Text="Update for c-time Universal!" 
                       Style="{StaticResource TitleTextBlockStyle}"
                       Foreground="{StaticResource SystemControlHighlightAccentBrush}" />
        </Viewbox>

        <Viewbox Grid.Row="2" StretchDirection="DownOnly" Margin="0, 3, 0, 0" HorizontalAlignment="Left">
            <TextBlock>
                <Run>The new version </Run>
                <Run Text="{x:Bind ViewModel.CurrentVersion}" />
                <Run> has been installed.</Run>
            </TextBlock>
        </Viewbox>
        
        
        <Rectangle Grid.Row="3" 
                   Fill="{ThemeResource BorderBrush}" 
                   Height="1" 
                   Margin="0, 10, 0, 10" />
        
        <RichTextBlock Grid.Row="4"
                       IsTextSelectionEnabled="False">
            <Paragraph>• Bla Blub alöskdjf öljaskd <Bold>följa</Bold> sdljköf aljsdf aklsd f!</Paragraph>
        </RichTextBlock>

    </Grid>
</Page>
