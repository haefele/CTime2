<Page
    x:Class="CTime2.Views.Settings.SettingsView"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CTime2.Views.Settings"
    xmlns:micro="using:Caliburn.Micro"
    xmlns:common="using:CTime2.Common"
    
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Pivot ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}"
               SelectedItem="{Binding ActiveItem, Mode=TwoWay}"
               >
            <Pivot.HeaderTemplate>
                <DataTemplate x:DataType="micro:Screen">
                    <TextBlock Text="{x:Bind DisplayName}" />
                </DataTemplate>
            </Pivot.HeaderTemplate>
            <Pivot.ItemTemplate>
                <DataTemplate x:DataType="micro:Screen">
                    <ContentControl micro:View.Model="{Binding}"
                                    VerticalContentAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch"
                                    />
                </DataTemplate>
            </Pivot.ItemTemplate>
        </Pivot>


    </Grid>
</Page>
